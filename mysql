#!/bin/bash

# jdbc:mysql://mysql-server.dev:3306/

USER_NAME=root
PASSWORD=root
DATA_PATH=/opt/databases/mysql5/data
CONTAINER=mysql-server5

docker rm -f $CONTAINER
docker run --name $CONTAINER --hostname mysql-server5.dev -p 3306:3306 \
	-e MYSQL_ROOT_PASSWORD=$PASSWORD -d \
	-v $DATA_PATH:/var/lib/mysql -v /opt/databases/mysql/conf.d:/etc/mysql/conf.d mysql:5.7.8
